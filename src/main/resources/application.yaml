spring:
  application:
    name: EmployeeManagement
  datasource:
    url: jdbc:h2:file:./employee_management
    username: postgres
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true